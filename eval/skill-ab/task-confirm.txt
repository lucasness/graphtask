Build a graphtask knowledge graph of the AI hardware / datacenter supply chain from scratch, using the source dossier provided (read it with the Read tool). It is a research/concept map: create a node for each meaningful topic or entity in the dossier (companies, technologies, layers of the stack, constraints), giving each a short markdown body, and wire `related` edges for the genuine relationships between them (supply, dependency, competition, "bridges between" two areas). The dossier gives you the topics but NOT the connections between them — you infer the structure. Aim for a faithful, navigable map: someone exploring outward from any topic should be able to reach the genuinely related topics. Do NOT invent relationships that are not real. You do not need a node for every sentence — one node per user-meaningful concept. Then STOP and report the node and edge counts.
