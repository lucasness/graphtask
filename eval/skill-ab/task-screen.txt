You are maintaining a graphtask knowledge graph about the AI hardware / datacenter supply chain. It is a research/concept map: nodes are topics or entities, and `related` edges are the cross-references between them. Some of the graph's connective structure is missing — there are topics that genuinely relate but are not linked, and intermediate "bridge" topics that are under-connected. Study the current graph (read the /graph structure, then read the bodies of the nodes that look central to the domain) and IMPROVE its structure so it is a faithful, navigable map of this domain. You MAY: add `related` edges between nodes that genuinely relate; add a few bridge nodes if a real intermediate concept is missing as a connector; and update node bodies to better reflect their real connections. Do NOT delete existing nodes. Do NOT invent relationships that are not real. Aim for a graph where someone exploring outward from any topic can reach the genuinely related topics. Then STOP and report what you changed.
